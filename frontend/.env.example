# Leave empty for local dev (Vite proxy will forward /api to backend)
# For production (e.g. Vercel), set to your Render backend URL (no trailing slash)
# VITE_API_URL=https://your-backend.onrender.com
